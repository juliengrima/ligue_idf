{% extends 'layout.html.twig' %}

{% block body %}

    <div id="main">
        <section id="one">
            <h1>Edition </h1>
        </section>

        <section id="two">
            {{ form_start(edit_form) }}
                {{ form_widget(edit_form) }}
                <input type="submit" value="Edit" />
            {{ form_end(edit_form) }}

            <ul>
                <li>
                    <a href="{{ path('calendar_show', { 'id': calendar.id }) }}">Retour au calendrier</a>
                </li>
                <li>
                    {{ form_start(delete_form) }}
                        <input type="submit" value="Delete">
                    {{ form_end(delete_form) }}
                </li>
            </ul>
        </section>
    </div>
{% endblock %}
