{% extends 'base.html.twig' %}

{% block header %}

    <!-- Header -->
    <header id="header">
        <div class="inner">
            <a href="{{ path('homepage') }}" class="image avatar"><img src="{{ asset('assets/images/avatar.jpg') }}" alt="" /></a>
            <h1><strong>I am Strata</strong>, a super simple<br />
                responsive site template freebie<br />
                crafted by <a href="http://html5up.net">HTML5 UP</a>.</h1>

            <div>
                {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                    <h3>{{ 'layout.logged_in_as'|trans({'%username%': app.user.username}, 'FOSUserBundle') }}</h3>
                {% endif %}
            </div>

            <div>
                <ul>
                    <li><a href="{{ path('homepage') }}">accueil</a></li>
                    <li><a href="{{ path('index_calendar') }}">Calendrier des matchs</a></li>
                    {% if is_granted('ROLE_ADMIN') %}
                    <li><a href="{{ path('homeadmin') }}">administrateur</a></li>
                    <li><a href="{{ path('fos_user_security_logout') }}" class="button">Déconnexion</a></li>
                    {% endif %}
                </ul>
            </div>
        </div>
    </header>

{% endblock %}

{% block footer %}

    <!-- Footer -->
    <footer id="footer">
        <div class="inner">
            <ul class="icons">
                <li><a href="#" class="icon fa-twitter"><span class="label">Twitter</span></a></li>
                <li><a href="#" class="icon fa-github"><span class="label">Github</span></a></li>
                <li><a href="#" class="icon fa-dribbble"><span class="label">Dribbble</span></a></li>
                <li><a href="#" class="icon fa-envelope-o"><span class="label">Email</span></a></li>
            </ul>
            <ul class="copyright">
                <li>
                    <a href="{{ path('fos_user_security_login') }}">&copy;</a> Untitled</li><li>Design: <a href="http://html5up.net">HTML5 UP</a>
                </li>
            </ul>
        </div>
    </footer>

{% endblock %}